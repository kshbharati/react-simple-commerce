import React from "react";

export default class AboutUs extends React.Component{
    render(): React.ReactNode {
        return(
            <main>
                <h2>About Us</h2>
            </main>
        );
    }
}